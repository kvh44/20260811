apiVersion: apps/v1
kind: Deployment
metadata:
  name: "20260819"
  labels:
    app: "20260819"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "20260819"
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: "20260819"
    spec:
      restartPolicy: Always
      containers:
      - name: "20260819"
        # IMAGE_PLACEHOLDER is replaced in CI with the built image (see .github/workflows/eks-deploy.yml)
        image: IMAGE_PLACEHOLDER
        imagePullPolicy: IfNotPresent
        env:
        - name: SERVER_PORT
          value: "8001"
        ports:
        - name: http
          containerPort: 8001
          protocol: TCP
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "256Mi"
        startupProbe:
          httpGet:
            path: /users
            port: http
          periodSeconds: 5
          timeoutSeconds: 2
          failureThreshold: 30
        livenessProbe:
          httpGet:
            path: /users
            port: http
          periodSeconds: 10
          timeoutSeconds: 2
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /users
            port: http
          periodSeconds: 5
          timeoutSeconds: 2
          failureThreshold: 3
