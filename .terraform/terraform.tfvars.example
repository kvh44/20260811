# Keep the defaults unless you also update task-definition.json. That file is
# rendered directly by GitHub Actions, so its family, role names, log group,
# region, container name, and port must remain aligned with these values.
aws_region          = "ca-central-1"
project_name        = "20260901"
ecr_repository_name = "20260901"
container_name      = "20260901"
container_port      = 8001
spring_profile      = "local"

# EKS, Argo CD, Helm, and GitHub Actions defaults are defined in variables.tf.
# Override them here only when your account already uses the same resource names.
# eks_cluster_name        = "eks-cluster-20260903"
# eks_ecr_repository_name = "20260903"
# github_repository       = "kvh44/20260811"
# github_repository_owner_id = "16013107"
# github_repository_id       = "1331272653"
# github_actions_terraform_role_name = "GitHubActionsTerraform-20260903"
